# Code Overview: Raftel vs. Damysus

This document describes the structure of the codebase and maps each major
component to the paper's claims.  It is intended to help artifact reviewers
understand what changed relative to the upstream
[Damysus](https://github.com/vrahli/damysus) baseline and why.

---

## Repository Layout

```
Raftel/
├── App/            Host-side C++ (untrusted, compiled with SGX SDK)
├── Enclave/        Trusted C++ (compiled into the SGX enclave)
├── Makefile        Top-level build; SGX_MODE=HW for paper runs
├── run.py          Experiment orchestrator (build, configure, launch, collect)
├── ae              AE CLI entry point
├── aliyun/         Alibaba Cloud instance lifecycle scripts
├── deployment/     Node provisioning and initialization scripts
├── experiments_reproduction/
│   ├── experiment1/script/run_wan.sh       Figure 3
│   ├── experiment2/script/run_lan.sh       Figure 4
│   └── experiment3/script/run_redis_wan.sh Figure 6
│       script/summarize_e2e.py
├── scripts/
│   ├── plot_fig3.py / plot_fig4.py / plot_fig6.py
│   └── gen_report.py
└── runs/reference/   Approximate reference values for PASS/WARN/FAIL
```

---

## Protocol Selection (compile-time)

`App/params.h` selects the protocol via preprocessor macros:

| Macro | Protocol | run.py flag |
|---|---|---|
| `BASIC_HYBRID_TEE` | Raftel | `--p0` |
| `CHAINED_HYBRID_TEE` | Chained-Raftel | `--p1` |
| `BASIC_ACHILLES` | Achilles | `--p2` |
| `BASIC_HOTSTUFF` | Hotstuff | `--p3` |
| `BASIC_DAMYSUS` | Basic-Damysus | `--p4` |

Each protocol is a separate branch of the original Damysus code, compiled
anew for each run.

---

## Key Source Files

### Host side (`App/`)

| File | Role |
|---|---|
| `Handler.cpp / .h` | Main consensus loop: message dispatch, phase transitions, quorum tracking, enclave ECALL wrappers |
| `KVApp.cpp / .h` | Redis-backed KV application layer; `KVAppCodec::encode` packs key+value+header into the fixed `PAYLOAD_SIZE` buffer |
| `Server.cpp` | Entry point for `sgxserver` / `server` |
| `Client.cpp` | Entry point for `sgxclient` / `client` |
| `Stats.cpp / .h` | Per-view throughput and latency statistics, written to `stats/vals-*` |
| `params.h` | Protocol selector macros and numeric constants (`PAYLOAD_SIZE`, `BATCH_SIZE`) |
| `config.h` | Node configuration (generated by `run.py`'s `mkConfig`) |

### Enclave side (`Enclave/`)

| File | Protocol | Role |
|---|---|---|
| `EnclaveComb.cpp` | Raftel | Trusted state machine: proposal validation, TEE signing, QC accumulation |
| `EnclaveChComb.cpp` | Chained-Raftel | Same for the chained variant |
| `EnclaveDamysus.cpp` | Basic-Damysus | Upstream Damysus enclave logic |
| `EnclaveOP.cpp` | Hotstuff | Hotstuff enclave logic |
| `EnclaveAchillesCh.cpp` | Achilles | Achilles enclave logic |
| `Enclave.edl` | — | ECALL/OCALL interface definition |

---

## Raftel vs. Damysus: What Changed

Raftel extends Damysus to support **partially trusted** topologies where only
a subset of replicas hold a TEE.  The key additions are:

### 1. TEE population parameter (`totaltee`)

`Handler.cpp` receives `totaltee` at startup (node argument position 2).
Replicas 0 … totaltee−1 are `TEE` nodes; the rest are `nonTEE`.  The TEE
quorum size `QT = max(totaltee/2 + 1, f+1)` governs when a certificate can
be issued without a full n−f classic quorum.

**Paper claim (§4–§5):** A TEE quorum of size `f+1` suffices for safety and
liveness when the leader is a TEE node; this is the `Raftel-Best` (S1)
configuration.

### 2. Raftel-Worst (`totaltee=0`)

Setting `--totaltee 0` disables the TEE fast path entirely (no TEE quorum is
ever reachable), forcing the protocol to fall back to the classic `3f+1`
quorum path.  This is the `Raftel-Worst` baseline in Figure 3.

**AE fix:** The original experiment script used `--totaltee f` (= `f` TEE
nodes, not zero), which is a different and stronger condition.  The correct
definition from §7.2 is `m=0`.

### 3. Fixed-leader mode (`--leader-mode fixed`)

`Handler.cpp` supports a `fixed` leader mode in which the leader is always
replica `leader_id` rather than rotating by view.  Figure 4 uses this to
separate the effect of TEE leadership (S1/S2, leader_id=0) from non-TEE
leadership (S3/S4, leader_id=f+1).

### 4. Redis KV application path

`App/KVApp.cpp` implements a Redis-backed key-value store behind the same
client interface.  `KVAppCodec::encode` serialises a `(key, value, op-type,
14-byte header)` tuple into the fixed `PAYLOAD_SIZE` byte buffer.  If the
tuple exceeds `PAYLOAD_SIZE`, `encode` returns `false` silently and the
request is dropped.

**AE fix (§7.6):** The original script passed `--payload 256
--kv-value-len 1024`; with keyspace=10000 (max key length 5 bytes),
`5+1024+14=1043 > 256`, so every request was silently dropped.  The correct
values are `--payload 1100 --kv-value-len 1024`.

---

## Paper Figure → Code Path

| Figure | Script | Key parameters | What it measures |
|---|---|---|---|
| Fig 3 | `run_wan.sh` | f∈{1…32}, 6 protocols, 50ms WAN | WAN scalability: throughput & latency vs. f |
| Fig 4 | `run_lan.sh` | f∈{1…32}, 4 TEE configs, LAN | Effect of TEE leader + TEE quorum size |
| Fig 6 | `run_redis_wan.sh` | f=8, clients∈{1…32}, 3 repeats, 50ms WAN | Redis E2E throughput-latency curve |

---

## SGX Simulation vs. Hardware Mode

`Makefile` accepts `SGX_MODE=SIM` (default for local smoke tests) or
`SGX_MODE=HW` (required for paper runs on SGX-capable cloud nodes).  The
experiment scripts pass `--sgx-mode HW` to `run.py`, which sets
`SGX_MODE=HW` in the make invocation and raises an error if make fails.

Local `./ae smoke` always uses SIM mode; `./ae run` passes HW mode.
